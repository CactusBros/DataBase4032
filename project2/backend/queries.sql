-- name: best_selling_product
SELECT * FROM product;

-- name: monthly_sales_by_category
SELECT * FROM product;

-- name: low_stock_alert
SELECT * FROM product;

-- name: top_customers
SELECT * FROM product;
